using Enums;
using StateMachine.Arguments;

public static class EventManager
{
    /*public delegate void AbilityApplyHandler(IUniversalArgument arg, ApplicationType t);

    public static event AbilityApplyHandler AbilityApply = delegate { };

    public static void OnAbilityApply(IUniversalArgument argument, ApplicationType t)
    {
        AbilityApply.Invoke(argument, t);
    }*/
}