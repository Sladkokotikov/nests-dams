using System;
using UnityEngine;


[ExecuteInEditMode]
public class CardConstructor : MonoBehaviour
{
    //private int _counter;

  
    private void Start()
    {
        
    }

    private void OnGUI()
    {
        /*if (GUI.Button(new Rect(10, 10, 150, 100), "I am a button"))
        {
            var ability = new ConfirmationAbility().Push().Confirm();
            foreach(var b in ability.Bytecode)
                print(b);
        }*/
    }
}